import {
  capitalizeString
} from 'app/helpers/capitalize-string';

module('CapitalizeStringHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = capitalizeString(42);
  ok(result);
});
